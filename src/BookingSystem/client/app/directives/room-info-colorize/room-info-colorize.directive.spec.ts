import { RoomInfoColorizeDirective } from './room-info-colorize.directive';

describe('RoomInfoColorizeDirective', () => {
  it('should create an instance', () => {
    const directive = new RoomInfoColorizeDirective();
    expect(directive).toBeTruthy();
  });
});
